CREATE DATABASE  test;
CREATE TABLE POST(
        id BEGIN AUTO_INCREMENT PRIMARY KEY,
        title VARCHAR(400) NOT NULL,
        content VARCHAR (2000) NULL
);

--CREATE TABLE Pracownicy (
 --   ID INT PRIMARY KEY,
  --  Imię VARCHAR(50),
  --  Nazwisko VARCHAR(50),
 --   Stanowisko VARCHAR(50),
 --   Pensja DECIMAL(10, 2)
--);
